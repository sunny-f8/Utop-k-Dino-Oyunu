PLAN: Replace bgmAudio with Web Audio API procedural tone generator

1. Lines 35-37: Replace
   OLD:
   let jumpAudio, hitAudio, winAudio
   let bgmAudio

   NEW:
   let jumpAudio, hitAudio, winAudio
   // Background music: Web Audio API procedural tone generator
   let bgmContext = null
   let bgmOscillator = null
   let bgmGain = null
   let bgmPlaying = false

2. Lines 528-551: Replace loadSounds() bgmAudio code with empty (just remove bgmAudio lines)

3. Add new helper functions before formatTime():
   - createBgmContext() - setup audioContext once
   - playBgmTone(freq, duration) - play single tone
   - playBgm() - start bgm sequence
   - pauseBgm() - stop bgm
   - resetBgm() - reset bgm state

4. Sequence: C4(262), D4(294), E4(330), C4(262) repeating, ~0.5s each note
